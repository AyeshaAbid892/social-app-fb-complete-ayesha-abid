# --- Sensitive / secrets ---------------------------------------------------
# NEVER commit real API keys. .env holds your actual OpenAI key — only
# .env.example (with a placeholder value) should ever be pushed.
.env
.env.local
.env.*.local
*.pem
*.key

# --- Dependencies ------------------------------------------------------------
node_modules/
.pnp
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# --- Build output -------------------------------------------------------------
dist/
dist-ssr/
build/
*.local

# --- Logs ----------------------------------------------------------------------
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# --- Editor / IDE ----------------------------------------------------------------
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- OS files -----------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# --- Vite / tooling caches ---------------------------------------------------
.vite/
*.tsbuildinfo

# --- Test / coverage -----------------------------------------------------------
coverage/
